# 🧹 Функция управления подписками

## 📋 Описание

Новая функция позволяет пользователям отслеживать и управлять подписками на каналы после завершения розыгрышей. Это помогает поддерживать чистоту ленты и не пропускать важные обновления.

## 🎯 Основные возможности

### 1. **Автоматическое отслеживание каналов**
- При добавлении розыгрыша автоматически запоминаются все каналы для подписки
- Каждый канал отслеживается отдельно для каждого пользователя
- Система запоминает, на какие каналы пользователь подписался специально для розыгрыша

### 2. **Умные напоминания**
- Через 1 день после завершения розыгрыша приходит напоминание об отписке
- Напоминание содержит список каналов и прямые ссылки для отписки
- Пользователь может отложить или проигнорировать напоминание

### 3. **Удобный интерфейс управления**
- Кнопка "🧹 Управление подписками" в главном меню
- Список завершенных розыгрышей с каналами для отписки
- Прямые ссылки на каналы для быстрой отписки
- Подтверждение отписки с отметкой в базе данных

### 4. **Аналитика отписок**
- Отслеживание количества отписанных каналов
- Статистика по очистке ленты
- История всех отписок

## 🔧 Технические детали

### База данных
Добавлены новые таблицы:
```sql
-- Отслеживание отписок
CREATE TABLE unsubscribe_tracking (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    giveaway_id INTEGER,
    user_id INTEGER,
    channel_name TEXT,
    channel_link TEXT,
    subscribed_before_giveaway BOOLEAN,
    unsubscribed_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Статистика отписок
CREATE TABLE unsubscribe_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    total_channels_tracked INTEGER DEFAULT 0,
    total_unsubscribed INTEGER DEFAULT 0,
    last_unsubscribe_date DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Модули
- `src/core/unsubscribe_manager.py` - основной модуль управления отписками
- Интеграция в `ultimate_giveaway_bot.py`

## 📱 Пользовательский интерфейс

### Главное меню
```
➕ Добавить розыгрыш    📋 Мои розыгрыши
✅ Проверить подписки   🧹 Управление подписками
📊 Экспорт данных      📈 Моя аналитика
🔔 Напоминания         📚 История
⚙️ Настройки
```

### Меню управления подписками
```
🧹 Управление подписками

Завершенные розыгрыши с каналами для отписки:

🎯 iPhone 15 Pro Max розыгрыш
📅 Завершен: 25.12.2024 20:00
📺 Каналы: @tech_channel, @gadgets_news

[Отписаться от iPhone 15 Pro Max розыгрыш...]
```

### Процесс отписки
1. Пользователь нажимает кнопку отписки
2. Открывается список каналов с прямыми ссылками
3. Пользователь переходит по ссылкам и отписывается от каналов
4. Возвращается в бот и нажимает "Подтвердить отписку"
5. Система отмечает каналы как отписанные

## 🚀 Использование

### Для пользователей
1. Добавляйте розыгрыши как обычно
2. Указывайте каналы для подписки
3. После завершения розыгрыша получите напоминание
4. Используйте "🧹 Управление подписками" для очистки ленты

### Для разработчиков
```python
# Инициализация менеджера отписок
unsubscribe_manager = UnsubscribeManager(bot, DB_NAME)

# Начало отслеживания каналов
unsubscribe_manager.track_giveaway_channels(giveaway_id, user_id, channels)

# Настройка напоминания
setup_unsubscribe_reminder(user_id, giveaway_id, title, end_date)

# Получение статистики
stats = unsubscribe_manager.get_user_stats(user_id)
```

## 📊 Статистика и аналитика

### Отслеживаемые метрики
- Количество отслеживаемых каналов
- Количество отписанных каналов
- Процент очистки ленты
- Время от завершения розыгрыша до отписки
- Популярные каналы для отписки

### Экспорт данных
Статистика отписок включается в общий экспорт Excel с отдельными листами:
- "Отписки" - детальная статистика
- "Каналы" - список всех отслеживаемых каналов
- "Аналитика отписок" - графики и диаграммы

## 🔮 Будущие улучшения

### Планируемые функции
- Автоматическая отписка через API (при возможности)
- Группировка каналов по тематикам
- Умные рекомендации по отписке
- Интеграция с календарем для планирования отписок
- Уведомления о новых постах в отслеживаемых каналах

### Технические улучшения
- Асинхронная обработка отписок
- Кэширование данных о каналах
- Оптимизация запросов к базе данных
- Интеграция с Telegram Bot API для проверки подписок

## 🛠️ Настройка и развертывание

### Требования
- Существующий бот с базой данных
- Модуль `unsubscribe_manager.py`
- Обновленная версия `ultimate_giveaway_bot.py`

### Установка
1. Скопировать файлы в проект
2. Импортировать модуль в основной бот
3. Инициализировать менеджер отписок
4. Добавить обработчики callback'ов

### Конфигурация
```python
# В config/env.example
UNSUBSCRIBE_REMINDER_DAYS=1  # Дней после завершения для напоминания
UNSUBSCRIBE_AUTO_CLEANUP=True  # Автоматическая очистка старых записей
```

## 📝 Примеры использования

### Базовое использование
```python
# Пользователь добавляет розыгрыш
add_giveaway(user_id, "iPhone розыгрыш", "iPhone 15", "25.12.2024", "@tech_channel")

# Система автоматически:
# 1. Отслеживает канал @tech_channel
# 2. Настраивает напоминание на 26.12.2024
# 3. Ждет завершения розыгрыша

# После завершения:
# 1. Отправляется напоминание
# 2. Пользователь может отписаться через меню
# 3. Система отмечает отписку в базе
```

### Расширенное использование
```python
# Получение статистики пользователя
stats = unsubscribe_manager.get_user_stats(user_id)
print(f"Отписано от {stats['total_unsubscribed']} каналов")

# Массовая отписка от всех каналов розыгрыша
unsubscribe_manager.mark_all_unsubscribed(giveaway_id, user_id)

# Получение рекомендаций
recommendations = unsubscribe_manager.get_recommendations(user_id)
```

## 🎉 Заключение

Функция управления подписками значительно улучшает пользовательский опыт, помогая поддерживать чистоту ленты и не пропускать важные обновления. Система автоматически отслеживает все подписки и напоминает о необходимости отписки после завершения розыгрышей.

---

*Документация создана: 06.10.2025*  
*Версия функции: 1.0*

